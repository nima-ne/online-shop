// next.config.js
/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["fakestoreapi.com"], // اضافه کن دامنه تصاویر خارجی رو
  },
  reactStrictMode: true,
}

module.exports = nextConfig;
